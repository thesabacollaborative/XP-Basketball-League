import { EmptyState } from "@/components/EmptyState";

export default function LeaderboardPage() {
  return (
    <EmptyState
      title="Leaderboard is empty"
      description="Rankings against real other users arrive in Phase 2."
    />
  );
}
