import { EmptyState } from "@/components/EmptyState";

export default function TeamsPage() {
  return (
    <EmptyState
      title="No teams yet"
      description="Team creation, rosters and crests arrive in Phase 2."
    />
  );
}
