import { EmptyState } from "@/components/EmptyState";

export default function CompetePage() {
  return (
    <EmptyState
      title="No competitions open"
      description="Real registration against tracked competitions arrives in Phase 2."
    />
  );
}
