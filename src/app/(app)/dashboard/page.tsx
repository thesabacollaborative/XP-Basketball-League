import { EmptyState } from "@/components/EmptyState";

export default function DashboardPage() {
  return (
    <EmptyState
      title="Your dashboard is empty for now"
      description="XP tracks, life pathways and active challenges land here in Phase 1."
    />
  );
}
